$kind: http-request
url: localhost:8000/api/CourseManagement/Course
method: POST
body:
  type: json
  content: |-
    {
        "name": "Tango"
    }
auth:
  type: bearer
  credentials:
    token: "{{Admin JWT}}"
order: 3000
