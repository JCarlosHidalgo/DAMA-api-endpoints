$kind: http-request
url: localhost:8000/api/CourseManagement/Course/schedule
method: GET
headers:
  Content-Type: application/json
auth:
  type: bearer
  credentials:
    token: "{{Admin JWT}}"
order: 13000
