$kind: http-request
url: localhost:8000/api/Auth/register/student
method: POST
body:
  type: json
  content: |-
    {
        "username":"Estudiante Nuevo",
        "password":"Admin123"
    }
auth:
  type: bearer
  credentials:
    token: "{{Admin JWT}}"
order: 5000
