$kind: http-request
name: obtain student credentials
url: localhost:8000/api/Credentials/studentCredentials
method: GET
body:
  type: json
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{Student JWT}}"
order: 3000
