$kind: http-request
name: obtain admin credentials
url: localhost:8000/api/Credentials/adminCredentials
method: GET
body:
  type: json
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{Admin JWT}}"
order: 1000
