$kind: http-request
name: obtain teacher credentials
url: localhost:8000/api/Credentials/teacherCredentials
method: GET
body:
  type: json
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{Teacher JWT}}"
order: 2000
